package si.um.feri.rest;
/*
public class ProduktController {
    public Uni <Response> putProduct(PathParam("id") int id, si.um.feri.dto.Produkt dto){
        return produktRepository.findById((long)id)
        .onIten().ifNull().failWith(new NotFoundException("produkt not found)
        .onItem().invoke(produkt->produkt.updateFrom(dto))
        .onItem().invoke(produktRepository::persist)
        .onItem().transform(produkt->Response.ok(produkt.toDto()).build());
        }
}



    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-reactive-panache</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-reactive-pg-client</artifactId>
    </dependency>
*/